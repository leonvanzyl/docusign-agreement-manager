Add a Docusign OAuth connection, completely separate from Better Auth. Create a standalone route handler at /api/docusign/callback - do NOT register Docusign through Better Auth or under /api/auth. Implement the Authorization Code Grant flow using the Integration Key and Secret in my env file. Add a "Connect Docusign" button that starts the consent flow; on callback, store the returned access and refresh tokens against the logged-in user. Do not write any direct Docusign REST calls - the token is only used to authorize the MCP connection in the next step.
https://developers.docusign.com/platform/mcp-server/?utm_campaign=AWA_FY27Q2&utm_medium=influencer-program&utm_source=Leon
